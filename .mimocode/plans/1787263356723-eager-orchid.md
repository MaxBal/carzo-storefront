# Carzo Checkout — Verification Plan

## Status: All Three Changes Are Already Implemented

After reading every file in the checkout system, all requested features are fully implemented by the previous Codex session. No code changes are needed — only verification.

---

## What's Implemented (Summary)

### Change 1 — Nova Poshta Delivery (BRANCH / POSTOMAT / COURIER)

| Layer | File | Status |
|-------|------|--------|
| Types | `lib/cart/types.ts` | `CheckoutDelivery` discriminated union for all 3 methods |
| Validation | `lib/cart/validation.ts` | Zod schemas for BRANCH, POSTOMAT, COURIER |
| UI | `components/cart/NovaPoshtaSelector.tsx` | 3-method radio group, city combobox, point dropdown, courier fields |
| API routes | `app/api/nova-poshta/{cities,points,streets}/route.ts` | All 3 endpoints with UUID validation |
| Server lib | `lib/nova-poshta.ts` | `findNovaPoshtaPoints` uses `CategoryOfWarehouse: 'Postomat'` from NP API |
| Server action | `app/actions/checkout.ts` | `resolveDelivery()` verifies all refs server-side |
| Order payload | `app/actions/checkout.ts` | All delivery fields written to Directus |
| Notifications | `lib/order-notifications.ts` | `deliveryDestination()` handles all methods |

Key behaviors verified in code:
- Postomat uses official NP API `CategoryOfWarehouse` filter (not string matching)
- Branch/postomat list loads immediately on dropdown open (0ms timeout for empty query)
- Courier has street autocomplete + house/apartment fields
- City change resets point/street/house (lines 259-271 of NovaPoshtaSelector.tsx)
- Method change preserves city but clears previous method data (lines 245-257)
- AbortController prevents race conditions on all fetch calls
- `allowPostomat` determined server-side by shipping dimensions (30x40x60cm, 20kg max)

### Change 2 — Mobile Header Hiding on Keyboard Open

| Layer | File | Status |
|-------|------|--------|
| Hook | `components/cart/useCartViewport.ts` | `visualViewport` API, keyboard detection, field visibility |
| CSS | `app/globals.css` lines 370-411 | Header `height: 0` + `translateY(-4rem)` with 200ms animation |
| Integration | `CartDrawer.tsx` line 44 | `useCartViewport(isOpen, checkout)` wired up |

Key behaviors verified in code:
- Keyboard detected when viewport shrinks by > `max(120px, 20% baseline)`
- Header animates up via CSS transform (not `display: none`)
- No reserved space after hiding (`height: 0`)
- Focus transitions between fields don't cause flicker (keyboard state persists via `keyboardOpen` flag)
- Submit footer also hides when keyboard is open
- `prefers-reduced-motion: reduce` respected
- Only activates on mobile (`max-width: 639px`) and during checkout mode

### Change 3 — Trust Row Under CTA

| Layer | File | Status |
|-------|------|--------|
| Component | `components/cart/NovaPoshtaTrustRow.tsx` | NP logomark + return policy text |
| Placement | `CartDrawer.tsx` line 266 | Directly after "Оформити замовлення" button |
| Asset | `public/nova-poshta-logomark-red.svg` | Official NP logomark (red, #DA292B) |

Text matches exactly: `Повернення та обмін на протязі 14 днів безкоштовно`

---

## Implementation Steps

### Step 1: TypeScript check
```bash
pnpm tsc --noEmit
```
Working directory: `product-page/`. Fix any type errors.

### Step 2: Lint
```bash
pnpm next lint
```
Working directory: `product-page/`. Fix any lint errors.

### Step 3: Build
```bash
pnpm build
```
Working directory: `product-page/`. Fix any build errors.

### Step 4: Report
Summarize pass/fail for each check, any errors fixed, and confirmation that all 3 features are complete.

No code changes are expected unless checks reveal errors.
